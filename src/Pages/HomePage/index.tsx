import * as React from 'react';

import './styles.css';

const HomePage: React.FC = (): JSX.Element => {
    return (
        <div>
            HOME PAGE
        </div>
    );
};

export default HomePage;
